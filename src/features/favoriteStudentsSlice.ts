// import { createSlice, PayloadAction } from '@reduxjs/toolkit';

// const initialState: string[] = [];

// const favoriteStudentsSlice = createSlice({
//   name: 'favoriteStudent',
//   initialState,
//   reducers: {
//     addFavoriteStudents: (state, action: PayloadAction<string>) => {
//       if (state.includes(action.payload)) {
//         return state.filter((id) => id !== action.payload);
//       } else {
//         state.push(action.payload);
//       }
//     },
//   },
// });

// export const { addFavoriteStudents }= favoriteStudentsSlice.actions;
// export default favoriteStudentsSlice.reducer;
